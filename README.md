# Karaoke Manager PWA - v12 Cloud Sync

## v12: Supabaseクラウド同期

Supabaseへのログインと端末間同期を追加しました。

### 接続先
- Project URL: `https://zrihlqlqgpbmtlhigceb.supabase.co`
- Publishable key: アプリ内に設定済み
- Secret / service_role key は使用していません

### ログイン
画面上部の「☁ クラウド」から、Supabase Authenticationに作成した
メールアドレス＋パスワードでログインします。

### 初回同期
- クラウドが空で端末に曲がある → 端末の曲をクラウドへアップロード
- 端末が空でクラウドに曲がある → クラウドから端末へダウンロード
- 両方に曲がある → IDと更新日時を見ながらマージ

### 通常同期
- 曲追加 / 編集 / お気に入り / タグ変更 / 点数追加などをクラウドへ反映
- 削除もクラウドへ反映
- オフライン時の変更は端末に残り、次回同期時に送信
- アプリ起動時（ログイン状態）とオンライン復帰時に自動同期
- 「今すぐ同期」ボタンでも同期可能

### ローカル保存も継続
IndexedDBは端末内キャッシュとして引き続き使用します。
クラウドに問題がある場合でも、端末内データとJSON/CSVバックアップを利用できます。

### セキュリティ
- ブラウザに入っているのは公開前提のPublishable keyだけです
- Supabase RLSでログイン中ユーザー自身のsongsだけ読み書きできます
- Database password / secret key / service_role key はアプリに含めません

## 既存機能
v11までの曲管理・DAM/JOYSOUND点数履歴・自動セッション・統計・タグ・定番曲・練習中・CSV/JSON・PWAなどはそのまま利用できます。


## v13 同期重複修正
- PCとスマホで同じ「曲名＋アーティスト＋キー」が別IDでも自動統合
- `songs_unique_song_key` の重複エラーを自動解消
- クラウド側のUUIDを基準に端末側IDを統一
- DAM / JOYSOUND点数履歴は重複を避けて結合
- タグは統合
- お気に入り / 定番曲 / 練習中はどちらかでONなら保持
- メモや自信ランク等は更新日時が新しい側を優先


## v14 バージョン表示
- 画面最上部の「KARAOKE MANAGER」の横に現在バージョンを小さく表示
- 今回は `v14`
- 今後のアップデート時もこの数字を更新し、PWAに最新版が反映されたか確認しやすくする


## v15 更新反映の高速化
- 最上部に「↻ 更新確認」ボタンを追加
- 公開中の `version.json` と現在のバージョンを比較
- 新版を検出した場合、Service Worker更新後に自動再読み込み
- 起動時にもバックグラウンドで更新を確認
- `index.html` / `app.js` / `style.css` / manifest / version情報はオンライン時ネットワーク優先
- オフライン時はキャッシュへフォールバック


## v16 同期安全性・自動化
- アプリ起動時だけでなく、スマホでアプリへ戻った時・PCでウィンドウを再表示した時にも自動同期
- 10秒の間隔制限で不要な連続同期を防止
- 前回同期時のクラウド状態を端末に保持し、3-way mergeで別端末の独立した編集を統合
- メモ等の同一項目を両端末で同時変更した場合は更新日時が新しい側を採用し、競合件数を表示
- タグとDAM/JOYSOUND点数履歴は端末ごとの追加内容を統合
- 削除競合ルールを明確化
  - 自端末で削除後にクラウド側でより新しい編集があれば編集を復元
  - 他端末で削除済みの曲を古い端末が編集していても、原則として削除を優先し誤復活を防止
- 同期結果に ↑送信 / ↓受信 / 削除 / 競合 件数を表示
- JSON書き出し時刻を記録し、最終バックアップ日時を表示
- 30日以上JSONバックアップしていない場合はバックアップ推奨を表示


## v17 曲追加フォーム操作
追加したショートカットは指定されたものだけです。

- テンキー `+`：キーを1上げる
- テンキー `-`：キーを1下げる
- キー未登録時は `+` で `+1`、`-` で `-1`
- キー変更範囲は `-6` ～ `+6`
- `Enter`：曲追加フォームの次の主要項目へ移動
- 日本語IMEの変換確定中のEnterはショートカットとして扱わない
- 連続追加モード中のみ `Insert`：保存して次の曲へ
- 通常追加・編集時の `Insert` には保存動作を追加しない
- 保存ボタンをDAM / JOYSOUND点数入力欄より上へ移動


## v18 メモ機能の整理
- 曲追加・編集画面から「メモ」を削除
- 曲詳細画面のメモ表示を削除
- 曲一覧のメモ表示を削除
- 検索対象からメモを削除
- Enterで次項目へ移動する順番からメモを削除
- 既存データとの互換性維持のため、内部データ / Supabase の `memo` 項目自体は残す
- 既存曲を編集しても過去のメモ値を不用意に消さない


## v19 複数選択・一括編集
- 曲一覧に「複数選択」モードを追加
- 選択中は曲カードをタップ / クリックして複数曲を選択
- 表示中の曲をまとめて全選択
- 選択解除 / 選択モード終了
- 選択曲へタグを一括追加
- 選択曲から指定タグを一括削除
- お気に入りを一括でON / OFF
- 定番曲を一括でON / OFF
- 練習中を一括でON / OFF
- 選択曲を一括削除
- キーや採点履歴は誤操作防止のため一括変更対象に含めない
- 一括編集した各曲は通常の編集と同様にクラウド同期対象になる


## v20 採点入力モード
- 一覧上部に「🎤 採点入力」を追加
- カラオケ中の採点登録に特化した簡易画面
- 曲名・アーティスト名から登録曲を高速検索
- 検索欄で Enter：先頭候補を選択
- 曲選択後は点数欄へ自動フォーカス
- 点数欄で Enter：「保存して次へ」
- 保存後は検索欄へ戻り、次の曲をすぐ入力可能
- 新しいセッションの最初だけ DAM / JOYSOUND を選択
- 最初の採点を保存した時点で機種を固定
- 既存仕様どおり「最初の採点から12時間」を同一セッションとして扱う
- 同一セッション中は採点入力モードの機種変更不可
- すでに進行中のセッションがある場合は、そのセッション最初の採点機種を自動使用
- 今回のセッションの入力履歴を最新8件表示
- 直近の点数をその場で修正・削除可能
- 通常のDAM / JOYSOUND履歴、歌唱回数、最終歌唱、統計、セッション履歴へそのまま反映
- 採点登録・修正・削除は通常の曲編集と同様にクラウド同期対象
- Supabaseのテーブル変更は不要


## v21 タグ色分け・未使用タグ整理
- タグを自動で色分け
- タグ名から色を決定するため、同じタグはPC・スマホで同じ色
- 曲一覧・曲詳細・よく使うタグ・タグ管理画面に色を反映
- 色設定をクラウド保存する必要がなく、Supabase SQL変更なし
- タグ管理画面に「未使用タグ ○件」を表示
- 曲に使われていない「よく使うタグ」も0曲としてタグ管理に表示
- 「未使用タグを整理」で、0曲のよく使うタグをまとめて削除
- 整理しても曲に実際に付いているタグは削除されない
